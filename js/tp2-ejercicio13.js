/*  13- Realiza un script que pida un texto y lo muestre en mayúsculas. */

const texto = prompt(`Ingrese su texto`);
const textoConvertido = texto.toUpperCase();

document.write(`<h2>${textoConvertido}</h2>`);

